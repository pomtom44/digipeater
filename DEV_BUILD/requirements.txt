fastapi
# Explicit floor, not just whatever fastapi pulls in transitively: PMTiles
# (the local map cache in services/tiles.py + web/server.py) needs Starlette's
# FileResponse to honor HTTP Range requests, only added in 0.39.0 and only
# safe from a real DoS (GHSA-7f5h-v6xp-fcq8, O(n^2) Range-header parsing) as
# of 0.49.1: see services/tiles.py.
starlette>=0.49.1
uvicorn[standard]
pillow
pyyaml
pyserial
httpx
# Real APRS packet decoding (MIC-E/compressed/timestamped/standard
# formats) for services/packet_log.py's heard-station/beacon tracking,
# same library ORIGINAL used, not reimplemented here.
aprslib
# Needed for FastAPI/Starlette to parse the login page's form POST (see
# web/server.py's /api/auth/login): required even for plain
# application/x-www-form-urlencoded bodies, not just multipart/file uploads.
python-multipart
