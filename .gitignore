# ── Config (contains callsign, passwords — never commit) ──────
config.yaml

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
dist/
build/

# ── Runtime data (stored outside repo on the Pi) ──────────────
*.log
*.json
# ...except the vendored MapLibre basemap style JSON below — *.json above
# is meant for runtime-generated files (display_config.json etc.), but
# matches these too unless carved out explicitly. They're static vendored
# assets (generated once via @protomaps/basemaps, not regenerated at
# install/runtime), so they need to actually be in the repo or the wizard's
# map step 404s trying to fetch them on a fresh clone.
!DEV_BUILD/web/static/maplibre/style-*.json


# ── Downloaded third-party binary (go-pmtiles, fetched by install.sh —
# see DEV_BUILD/services/tiles.py) and the region/world extracts it
# produces ──
DEV_BUILD/bin/
DEV_BUILD/map_data/

# ── Basemap fonts/sprites (protomaps/basemaps-assets, fetched by
# install.sh — see DEV_BUILD/web/static/first_run.html's MapLibre style
# loading) ──
DEV_BUILD/web/static/maplibre-assets/

# ── Direwolf generated config ─────────────────────────────────
direwolf.conf

# ── Legacy ────────────────────────────────────────────────────
digipeater_legacy-main/

# ── Third-party vendor software (not ours to redistribute) ────
# Alinco's official DR_X38.exe + Prolific PL-2303 driver installers, kept
# locally only as a reference for reverse-engineering hardware/radio_programmer.py.
alinco/

# ── IDE / OS ──────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
